# Variables de entorno para desarrollo local
# Copia este archivo como .env.local y completa con tus credenciales

# Vercel Postgres (Neon) URLs
# Obtén estas URLs desde: https://vercel.com/dashboard → tu-proyecto → Storage → Postgres → .env.local tab
POSTGRES_URL=""
POSTGRES_PRISMA_URL=""
POSTGRES_URL_NO_SSL=""
POSTGRES_URL_NON_POOLING=""
POSTGRES_USER=""
POSTGRES_HOST=""
POSTGRES_PASSWORD=""
POSTGRES_DATABASE=""

# Twilio WhatsApp Notifications
# Obtén estas credenciales desde: https://console.twilio.com
TWILIO_ACCOUNT_SID=""           # Account SID desde Twilio Console
TWILIO_AUTH_TOKEN=""            # Auth Token desde Twilio Console
TWILIO_WHATSAPP_FROM=""         # Ej: whatsapp:+14155238886 (número de Twilio Sandbox)
TWILIO_WHATSAPP_TO=""           # Ej: whatsapp:+34722224379 (tu número verificado)

# Email Configuration (Gmail)
# Guía completa en: EMAIL_CALENDAR_SETUP.md
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=""                   # Tu dirección de Gmail
EMAIL_PASSWORD=""               # Contraseña de aplicación de Gmail (16 caracteres)
EMAIL_FROM=""                   # Desde qué email se envían las notificaciones
EMAIL_TO=""                     # Email donde recibes las notificaciones de reservas

# Google Calendar Configuration
# Guía completa en: EMAIL_CALENDAR_SETUP.md
GOOGLE_CALENDAR_ID=""           # ID del calendario (ej: abc123@group.calendar.google.com)
GOOGLE_SERVICE_ACCOUNT_EMAIL="" # Email de la cuenta de servicio
GOOGLE_PRIVATE_KEY=""           # Clave privada (debe incluir "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n")

